#服务器端口号
#server:
#    port: 9091

#数据源基本配置信息
spring:
#  datasource:
#    driver-class-name: com.mysql.jdbc.Driver
#    url: jdbc:mysql://localhost:3306/limo_vue?characterEncoding=utf8
#    username: root
#    password: 1234

  #JPA配置
#  jpa:
#    database-platform: org.hibernate.dialect.MySQL5Dialect
#    show-sql: true
#    hibernate:
#      ddl-auto: update
#
#  jackson:
#    serialization:
#      indent_output: false

  #当前环境
  profiles:
    active: dev

#mybatis配置
#mybatis:
  #type-aliases-package: com.limo_vue.domain
  #config-location: classpath:mybatis/mybatis-config.xml
  #mapper-locations: com.limo_vue.dao
  #mapper-locations: com.limo_vue.dao



  #redis的相关配置
  redis:
   # Redis数据库索引（默认为0）
   # Redis服务器地址
   host: 127.0.0.1
   # Redis服务器连接端口
   port: 6379
   # Redis服务器连接密码（默认为空）
   password:
   # 连接池最大连接数（使用负值表示没有限制）
   pool:
    max-active: 8
   # 连接池最大阻塞等待时间（使用负值表示没有限制）
    max-wait: -1
   # 连接池中的最大空闲连接
    max-idle: 8
   # 连接池中的最小空闲连接
    min-idle: 0
   # 连接超时时间（毫秒）
   timeout: 0
